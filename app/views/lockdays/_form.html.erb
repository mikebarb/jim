<%= form_for(lockday) do |f| %>
  <% if lockday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lockday.errors.count, "error") %> prohibited this lockday from being saved:</h2>

      <ul>
      <% lockday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %>
    <%= f.date_select :day %>
  </div>

  <div class="field">
    <%= f.label :locked %>
    <%= f.select :locked, [TRUE, FALSE] %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.hidden_field :user_id, :value => @current_user_id, :readonly => true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
