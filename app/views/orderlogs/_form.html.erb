<%= form_for(orderlog) do |f| %>
  <% if orderlog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(orderlog.errors.count, "error") %> prohibited this orderlog from being saved:</h2>

      <ul>
      <% orderlog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product %>
    <%= f.text_field :product %>
  </div>

  <div class="field">
    <%= f.label :shop %>
    <%= f.text_field :shop %>
  </div>

  <div class="field">
    <%= f.label :day %>
    <%= f.date_select :day %>
  </div>

  <div class="field">
    <%= f.label :user %>
    <%= f.text_field :user %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
