<p id="notice"><%= notice %></p>

<h1>Bake to fill the Orders to</h1>
<h1>Deliver On : <%= @current_day %> (<%= Date.parse(@current_day).strftime("%A") %>)</h1>

<table>
    <thead>
    <tr>
      <th>id</th>
      <th>Product_id</th>
      <th>Shop_id</th>
      <th>quantity</th>
    </tr>
  </thead>
  <tbody>
    <% @baker1.each do |baker1| %>
      <tr>
        <td><%= baker1.id %></td>
        <td><%= baker1.product_id %></td>
        <td><%= baker1.shop_id %></td>
        <td><%= baker1.quantity %></td>
      </tr>
    <% end %>
  </tbody>
</table>



<table>
  <thead>
    <tr>
      <th>Product</th>
      <th>Quantity</th>
    </tr>
  </thead>

  <tbody>
    <% @bakers.each do |baker| %>
      <tr>
        <td><%= baker.title %></td>
        <td><%= baker.totalqty %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_order_path %>
<%= link_to 'Edit Order', ordersedit_path %>

